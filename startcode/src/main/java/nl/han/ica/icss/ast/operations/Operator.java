package nl.han.ica.icss.ast.operations;

public enum Operator {
    EQ,  // Equals
    NEQ, // Not equals
    GT,  // Greater then (>)
    GTE, // Greather then or equals (>=)
    ST,  // Smaller then (<)
    STE  // Smaller then or equals (<=)
}
